{
  "buildCommand": "cd artifacts/birthday-site && npm install && npm run build",
  "outputDirectory": "artifacts/birthday-site/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
